#include <stdio.h>
#include <iostream>

int main(int argc, char const *argv[])
{
    int discos = 3;

    moveDisco(discos, "A", "C", "B");
    // function(qtd, "origem", "destino", "auxiliar");

    return 0;
}
z
void moveDisco(int n, char ori, char dest, char aux)
{
    if (n == 1)
        cout<<"Mover disco"
    else
    {

    }
}
