#include <stdio.h>
#include <stdlib.h>
#include "main.h"

int main()
{
    lista Lista;

    *v = criaVetor(&Lista);
    buscaSequencial();

    return 0;
}

/*  if (buscaVetor(p, n, 0) != 1)
    printf("Not found"); */

/*  if (buscaVetor(p, 0, 30) != 1)
    printf("Not found"); */